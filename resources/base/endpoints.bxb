endpoints {  
  action-endpoints {
    action-endpoint (GetWifiLocation) {
      accepted-inputs (near, point, self)
      local-endpoint (GetWifiInfo.js)
    }

    action-endpoint (GetLocalinfoFromLocalName) {
      accepted-inputs (point)
      local-endpoint (GetLocalinfoFromLocalname.js)
    }
  }
}