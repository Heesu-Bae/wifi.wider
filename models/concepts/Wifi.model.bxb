structure (Wifi) {
  description (Wifi 정보를 담는 structure입니다)
  property (wifissid) {
    type (WifiSSID) // wifi ssid
    min (Required) max (One)
  }
  
  property (rdnmadr) {
    type (Rdnmadr) // 도로명주소
    min (Required) max (One)
  }
  
  property (lnmadr) {
    type (Lnmadr) // 지번주소
    min (Required) max (One)
  }

  property (latitude) {
    type (Latitude) // 위도
    min (Required) max (One)
  }

  property (longitude) {
    type (Longitude) // 경도
    min (Required) max (One)
  }
}
